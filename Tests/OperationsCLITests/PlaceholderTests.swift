import Testing

@testable import OperationsCLI

/// Trivially-true placeholder confirming the `OperationsCLITests` target
/// builds and runs against `OperationsCLI`.
@Test func placeholderPasses() {
    #expect(true)
}
