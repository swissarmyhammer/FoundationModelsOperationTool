---
comments:
- actor: wballard
  id: 01kwhxcmra1g3jm644tvyaqqvy
  text: "Implemented via TDD. \n\n**What was built:**\n- `Sources/Operations/Operations.swift`: real `@attached(extension, conformances: OperationDefinition, names: ...)` declaration for `@Operation(verb:noun:description:)`, plus new `@attached(peer) @OperationParam(short:aliases:allowedValues:)` marker macro declaration.\n- `Sources/OperationsMacros/OperationsMacros.swift`: `OperationMacro: ExtensionMacro` synthesizes an `extension X: OperationDefinition { static let verb/noun/operationDescription; static let parameterMetadata: [ParamMeta] }` by walking stored properties. Type mapping (String/Int/Double/Float/Bool, `[T]`, `Optional` ⇒ not required), description from literal `@Guide(description:)` else `///` doc-comment trivia else `\"\"`, `allowedValues` from `@OperationParam(allowedValues:)` else a recognized literal `@Guide(.anyOf([...]))`, short/aliases from `@OperationParam`. Diagnostics (all `.error` severity): non-struct target, empty/missing `verb`/`noun` literal, reserved parameter name `op` (normalizes lowercase + strips `_`/`-`, so `_Op`/`O-P` are caught too), unsupported field type. `OperationParamMacro: PeerMacro` expands to nothing (pure marker read by `OperationMacro`). Command-emission (ArgumentParser leaf) is explicitly out of scope per the task description — left to the follow-on task.\n- `Package.swift`: added `SwiftSyntax`/`SwiftSyntaxBuilder`/`SwiftSyntaxMacroExpansion`/`SwiftSyntaxMacrosTestSupport` deps to `OperationsMacrosTests`.\n- Tests: `Tests/OperationsMacrosTests/OperationMacroTests.swift` — 14 `assertMacroExpansion` fixtures (simple op, optional/array fields, Int/Double/Bool/required-array types, unit struct, `@OperationParam` short/aliases, `@Guide(.anyOf(...))`, doc-comment fallback, reserved-`op` + normalizes-to-`op` diagnostics, unsupported-type diagnostic, empty verb/noun diagnostics, non-struct diagnostic). `Tests/OperationsTests/OperationMacroIntegrationTests.swift` — a real `@Generable @Operation(...)` struct compiled end-to-end (not simulated) proving conformance, parameterMetadata, and `AnyOperation` dispatch all work under the actual compiler.\n\n**Process notes:** Wrote fixtures first against the extension-macro stub (which returned `[]`), watched them fail, then implemented. Diagnostic line/column expectations were calibrated against real `assertMacroExpansion` output (3 off-by-one/line guesses corrected) rather than hand-computed — standard for macro snapshot tests. An adversarial double-check pass flagged a real coverage gap (no fixture exercised Int/Double/Bool/required-array types); added `numericBooleanAndRequiredArrayFieldsMapToTheirParamTypes` to close it.\n\n**Verification:** `swift build` — 0 errors, 0 warnings. `swift test` — 28/28 tests pass across all three test targets (13 OperationsTests incl. 3 new integration tests, 14 OperationsMacrosTests, 1 OperationsCLITests), 0 failures, 0 warnings.\n\nLeaving task in `doing` for review per the implement workflow."
  timestamp: 2026-07-02T17:17:56.362066+00:00
- actor: wballard
  id: 01kwhxzxbwz3602q1bvb4s76wt
  text: |-
    Fixed review finding: updated the unsupported-type diagnostic message in Sources/OperationsMacros/OperationsMacros.swift to list 'Float' among supported types (was missing it despite the type-mapping switch explicitly handling `Float` -> `.number`). Also updated the matching assertMacroExpansion fixture in Tests/OperationsMacrosTests/OperationMacroTests.swift (unsupportedFieldTypeProducesDiagnostic) to expect the corrected message text.

    Verification: clean build (`rm -rf .build && swift build`) — 0 errors, 0 warnings. `swift test` — 28/28 tests pass (13 OperationsTests, 14 OperationsMacrosTests, 1 OperationsCLITests), 0 failures, 0 warnings.

    Checklist item checked off. Leaving task in `doing` per implement workflow — ready for /review.
  timestamp: 2026-07-02T17:28:27.772510+00:00
- actor: wballard
  id: 01kwhz3bdxa6tc0j7hbatgzfds
  text: |-
    Fixed the 5 review findings dated 2026-07-02 12:31, all in Sources/OperationsMacros/OperationsMacros.swift:

    1. Extracted `argumentLists(forAttributeNamed:in:)`, shared by `guideInfo(from:)` and `operationParamInfo(from:)` to eliminate the duplicated attribute-finding guard pattern.
    2. Extracted `extractStringArrayLiterals(from expr: ExprSyntax) -> [String]?` (casts to `ArrayExprSyntax` and compactMaps string literals), used by both the `aliases` and `allowedValues` cases in `operationParamInfo`.
    3. Added a full doc comment (parameters + return value) to `OperationParamMacro.expansion`.
    4. Split `OperationMacro.expansion` (was ~95 lines) into `expansion` (now 42 lines) plus four new private helpers: `verbNounDescriptionText(from:node:in:)`, `synthesizeParameterMetadata(from:in:)`, `formatParameterMetadataText(_:)`, and `generateExtensionMembersText(accessText:verbText:nounText:descriptionText:parameterMetadataText:)`.
    5. Added a full doc comment (parameters + return value) to `OperationMacro.expansion`.

    Adversarial double-check (via the `double-check` agent, run twice) caught a real regression in the first pass: the new `allowedValues = extractStringArrayLiterals(from: argument.expression)` line unconditionally overwrote `allowedValues` to `nil` when the expression wasn't an array literal, unlike the original code (and unlike the sibling `aliases` case) which left the prior value untouched on a failed cast. Fixed with `?? allowedValues` to match `aliases`'s `?? aliases` fallback. The same pass also flagged that `expansion`'s body was still ~60 lines (short of the ~40 target), so the verb/noun/description validation-and-derivation block was further extracted into `verbNounDescriptionText(from:node:in:)`, bringing `expansion` down to 42 lines. Re-run of double-check on both fixes: PASS.

    Verification: clean `rm -rf .build && swift build` — 0 errors, 0 warnings. `swift test` — 28/28 passing (13 OperationsTests, 14 OperationsMacrosTests, 1 OperationsCLITests), 0 failures, 0 warnings.

    All 5 checklist items in the 2026-07-02 12:31 review-findings section are checked off. Leaving task in `doing` per the implement workflow — ready for /review.
  timestamp: 2026-07-02T17:47:49.053618+00:00
- actor: wballard
  id: 01kwj0ckbbyb0f5b8wtfhzrm2w
  text: |-
    Fixed the 5 review findings dated 2026-07-02 12:50, all in Sources/OperationsMacros/OperationsMacros.swift (round 3 dedup pass):

    1. `operationParamInfo(from:)`'s `aliases`/`allowedValues` switch cases were near-identical. Replaced with a data table (`operationParamArrayArgumentLabels = ["aliases", "allowedValues"]`) plus a `[String: [String]]` accumulator, looped generically over both labels instead of one hand-written case per label. `short` stays its own branch since it's a different shape.
    2. The empty-string-literal source text `"\"\""` appeared 3 times in `verbNounDescriptionText`. Extracted `private let emptyStringLiteralText = "\"\""`, used in all three defaults.
    3. The two `unsupportedParameterType` diagnostic call sites in `synthesizeParameterMetadata` (missing type annotation vs. unmappable type) differed only in the diagnosed location. Extracted `diagnoseUnsupportedParameterType(_:at:in:)`.
    4. The `aliases`/`allowedValues` conditional-append blocks that built the `ParamMeta(...)` args array were near-identical. Extracted `appendArrayArgument(to:key:values:)` for the shared "append when non-empty" case.
    5. The verb/noun non-empty validation blocks were near-identical copies differing by expression and diagnostic case. Extracted `validateNonEmptyStringArgument(expr:diagnostic:node:in:)`, called twice.

    **Regression caught and fixed via adversarial double-check:** my first pass routed `allowedValues` through the same `appendArrayArgument` helper as `aliases`, gating both on "non-empty". But the *original* code gated `allowedValues` on `if let allowedValues` (non-nil only) — an explicit `@OperationParam(allowedValues: [])` used to emit `ParamMeta(..., allowedValues: [])` (an empty closed set), which the naive helper would have silently changed to omitting the argument (defaulting to `nil`, i.e. unconstrained). Fixed by splitting `appendArrayArgument` into `arrayArgumentText(key:values:)` (pure formatting) + a still-non-empty-gated `appendArrayArgument` for `aliases`, while `allowedValues` now does `if let allowedValues { args.append(arrayArgumentText(...)) }` inline, preserving the original nil-vs-empty distinction. Added a new pinning test `explicitEmptyAllowedValuesIsPreservedRatherThanOmitted` in Tests/OperationsMacrosTests/OperationMacroTests.swift. Re-ran double-check after the fix: PASS, with the reviewer independently re-running the test suite.

    Verification: `rm -rf .build && swift build` — 0 errors, 0 warnings. `swift test` — 29/29 passing (13 OperationsTests across 2 suites, 15 OperationsMacrosTests, 1 OperationsCLITests), 0 failures, 0 warnings.

    All 5 checklist items in the 2026-07-02 12:50 review-findings section are checked off. Leaving task in `doing` per the implement workflow — ready for /review.
  timestamp: 2026-07-02T18:10:20.651292+00:00
- actor: wballard
  id: 01kwj13kx9851ranjb0hmy160m
  text: |-
    Fixed the review finding dated 2026-07-02 13:13 in Sources/OperationsMacros/OperationsMacros.swift:

    Per rule of three, `appendArrayArgument(to:key:values:)` had only one caller (the `aliases` case inside `synthesizeParameterMetadata`'s property loop; `allowedValues` used a separate `if let allowedValues { args.append(arrayArgumentText(...)) }` path). Inlined the single call site to `if !operationParam.aliases.isEmpty { args.append(arrayArgumentText(key: "aliases", values: operationParam.aliases)) }` and removed the now-dead `appendArrayArgument` helper function (and its doc comment) entirely. `arrayArgumentText(key:values:)` itself stays — it has two genuine call sites (`aliases` and `allowedValues`) and does real formatting work.

    Verification: clean `rm -rf .build && swift build` — 0 errors, 0 warnings. `swift test` — 29/29 passing (13 OperationsTests across 2 suites, 15 OperationsMacrosTests, 1 OperationsCLITests), 0 failures, 0 warnings.

    Checklist item checked off. Leaving task in `doing` per the implement workflow — ready for /review.
  timestamp: 2026-07-02T18:22:54.889475+00:00
depends_on:
- 01KWHQCVGNFHVT0ZKEDAG802RR
position_column: doing
position_ordinal: '80'
title: '@Operation macro (metadata): conformance, ParamMeta synthesis, diagnostics'
---
## What\nIn `Sources/OperationsMacros/` (impl) + `Sources/Operations/` (declarations): attached extension+member macro `@Operation(verb:noun:description:)` and marker attribute `@OperationParam(short:aliases:)` — the metadata half only (the nested ArgumentParser `Command` emission is a separate follow-on task).\nEmits on the annotated struct:\n- `OperationDefinition` conformance with `static verb/noun/operationDescription`\n- `static parameterMetadata: [ParamMeta]` from stored properties: type mapping (String/Int/Double/Bool/[T], `Optional` ⇒ not required), description from literal `@Guide(description:)` argument (fallback: doc-comment `///` trivia), short/aliases from `@OperationParam`, allowedValues from `@OperationParam` or a recognized literal `@Guide(.anyOf([...]))` — all other `@Guide` constraint forms ignored in ParamMeta (left to Apple's schema)\nDiagnostics: unsupported field types, missing verb/noun, reserved parameter name `op` (including names that normalize to `op`).\n\n## Acceptance Criteria\n- [x] A `@Generable @Operation(...)` struct gains OperationDefinition conformance and a correct parameterMetadata table\n- [x] A field documented only by `///` doc comment (no `@Guide`) gets that text as its ParamMeta description\n- [x] Reserved-name `op` field produces a compile-time diagnostic\n\n## Tests\n- [x] `Tests/OperationsMacrosTests/OperationMacroTests.swift` — `assertMacroExpansion` fixtures: simple op; optional/array fields; unit struct (no fields); `@OperationParam` short/aliases; `@Guide` with constraint args; **doc-comment-trivia description fallback (no @Guide)**; reserved-`op` diagnostic; unsupported-type diagnostic\n- [x] Run `swift test --filter OperationMacroTests`; all pass\n\n## Workflow\n- Use `/tdd` — write failing tests first, then implement to make them pass.\n\n## Implementation Notes (2026-07-02)\n\nImplemented `OperationMacro: ExtensionMacro` and `OperationParamMacro: PeerMacro` in `Sources/OperationsMacros/OperationsMacros.swift`, with real macro declarations (replacing the stubs) in `Sources/Operations/Operations.swift`. Also added a required end-to-end integration test (`Tests/OperationsTests/OperationMacroIntegrationTests.swift`) applying `@Operation`/`@OperationParam`/`@Guide`/`@Generable` together and compiling for real, plus an extra fixture covering Int/Double/Bool/required-array type mapping (found missing by adversarial review). Command-emission (ArgumentParser leaf) intentionally deferred to the follow-on task per the \"What\" section above.\n\nFinal verification: `swift build` 0 errors/0 warnings; `swift test` 28/28 passing across all three test targets, 0 warnings.\n\n## Review Findings (2026-07-02 12:20)\n\n- [x] `Sources/OperationsMacros/OperationsMacros.swift:42` — Error message for unsupported types lists supported types as 'String, Int, Double, Bool' but the code also explicitly maps 'Float' to '.number' (line 100), making the error message incomplete and potentially misleading to users. Update the error message to include Float: 'parameter... supports String, Int, Double, Float, Bool, Array of those, and Optional wrapping any of those'.\n\n## Review Findings (2026-07-02 12:31)\n\nScope: HEAD~1..HEAD (fix commit 53a731b998bbb5b732f44aad2e846389e8721f83 \"fix(macro): include Float in unsupported-type diagnostic message\")\n\n- [x] `Sources/OperationsMacros/OperationsMacros.swift:169` — Attribute-finding guard pattern duplicated: both guideInfo (line 169-171) and operationParamInfo (line 207-209) contain identical guards to find an attribute by name and extract its arguments. This boilerplate repeats the same structure with only the attribute name ('Guide' vs 'OperationParam') changing. Extract a helper function that takes an attribute name and yields (argument, index) pairs for that attribute, eliminating the duplicated guard-and-iterate structure.\n- [x] `Sources/OperationsMacros/OperationsMacros.swift:220` — String array extraction pattern duplicated within operationParamInfo: lines 220-222 (aliases) and 225-227 (allowedValues) both use 'arrayExpr.elements.compactMap { $0.expression.plainStringLiteralValue }' with only the target variable changing. Extract a helper function extractStringArrayLiterals(from: ArrayExprSyntax) -> [String]? to eliminate the duplicated pattern.\n- [x] `Sources/OperationsMacros/OperationsMacros.swift:248` — Public function `expansion` lacks documentation for its parameters and behavior — while the struct documentation explains overall purpose, the function-level parameters (node, declaration, type, protocols, context) and return value (ExtensionDeclSyntax array) need explanation. Add function-level documentation explaining the expansion parameters and that it returns an array of extension declarations.\n- [x] `Sources/OperationsMacros/OperationsMacros.swift:256` — The `OperationMacro.expansion` method is approximately 95 lines of actual code, exceeding the ~50 line threshold. Long methods are harder to test, understand, maintain, and reuse. Extract the property-to-metadata-entry logic (lines 293–345, the nested for loops) into a separate helper method such as `synthesizeParameterMetadata(from structDecl)`. Extract the metadata text formatting (lines 348–354) into a helper like `formatParameterMetadataText(entries)`. Extract the extension member text construction (lines 356–364) into a helper like `generateExtensionMembersText(verb, noun, description, metadata)`. This will reduce the main method to ~40 lines.\n- [x] `Sources/OperationsMacros/OperationsMacros.swift:330` — Public function `expansion` lacks documentation for its parameters and behavior — while the struct documentation explains overall purpose, the function-level parameters and return value need explanation. Add function-level documentation explaining the expansion function's parameters and return value.\n\n## Review Findings (2026-07-02 12:50)\n\nScope: HEAD~1..HEAD (refactor commit extracting helpers, splitting expansion function, adding docs)\n\n- [x] `Sources/OperationsMacros/OperationsMacros.swift:230` — The `aliases` and `allowedValues` case statements in the switch of `operationParamInfo` (lines 229–230 and 231–232) are near-verbatim copies that differ only in the variable name. Both perform the identical operation: `variable = extractStringArrayLiterals(from: argument.expression) ?? variable`. These should be consolidated or extracted into a parameterized helper. Restructure to use a single case or extract the common assignment pattern into a helper that handles both variables, or use a dictionary-based approach to handle multiple fields uniformly.\n- [x] `Sources/OperationsMacros/OperationsMacros.swift:350` — The literal `\"\\\"\\\"\"` (empty string source representation) appears three consecutive times at lines 350–352 as default values. Should be extracted to a named constant to clarify intent and enable single-point changes. Extract to a module-scoped constant (e.g., `private let emptyStringLiteralText = \"\\\"\\\"\"`) and use it in all three initializers.\n- [x] `Sources/OperationsMacros/OperationsMacros.swift:353` — The `unsupportedParameterType` diagnostic calls at lines 353–355 and 370–372 are near-verbatim copies that differ only in the location parameter passed to `.diagnose(at:)` — `binding` vs `typeAnnotation`. Both emit the same diagnostic with identical structure; the difference should be parameterized into a shared helper. Extract a helper function `diagnoseUnsupportedParameterType(_:at:context:)` that both call sites can use, parameterizing the location.\n- [x] `Sources/OperationsMacros/OperationsMacros.swift:388` — The `aliases` conditional block (lines 388–390) and `allowedValues` conditional block (lines 392–394) are near-verbatim copies that differ only in the collection variable name and the argument key. Both follow the identical pattern: `if condition { let varText = collection.map(swiftStringLiteral).joined(...); args.append(\"key: [\\(varText)]\") }`. This should be extracted into a shared helper function. Extract a helper function `appendArrayArgument(to:key:values:)` that takes the args array, key name, and optional values collection, then calls it for both cases.\n- [x] `Sources/OperationsMacros/OperationsMacros.swift:409` — The two validation blocks for `verb` and `noun` in `verbNounDescriptionText` are near-verbatim copies that differ only in the variable name and the diagnostic case emitted. This duplication should be extracted into a shared helper function parameterized by the expression, diagnostic case, and node location. Extract a helper function `validateNonEmptyStringArgument(expr:diagnostic:node:context:)` that performs the shared check and diagnosis, then call it twice with the appropriate expressions and diagnostic cases.\n\n## Review Findings (2026-07-02 13:13)\n\nScope: HEAD~1..HEAD (refactor commit dd98667 \"eliminate remaining duplication in param/diagnostic handling\")\n\n- [x] `Sources/OperationsMacros/OperationsMacros.swift:329` — appendArrayArgument is a needless helper wrapping a single call site. It is called exactly once (line 399) and adds no meaningful abstraction — the guard isEmpty check and append operation are straightforward. Per the rule of three, a helper introduced for a *real* second (or third) call site is warranted; with one caller, this is indirection without payoff. Inline the call at line 399 to: if !operationParam.aliases.isEmpty { args.append(arrayArgumentText(key: \"aliases\", values: operationParam.aliases)) }.\n